import React from "react";
import styled from "styled-components";

const Padding = styled.div`
  width: auto;
  height: 44px;
`

function ComponentsPadding() {
  return (
    <Padding/>
  )
}

export default ComponentsPadding;